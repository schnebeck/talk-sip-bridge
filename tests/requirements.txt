# Only for the static check in test_static.py, which skips itself when
# this is absent. Deliberately not in bridge/requirements.txt: nothing
# the daemon runs needs it, and a deployment venv should carry nothing
# it does not run.
ruff
